server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/employee_management_system
    username : sanjay
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true